/** The wordmark. Split so the header can accent the TLD. */
export const brandCopy = {
  /** The wordmark is split so the header can accent the TLD. */
  name: 'educacion',
  tld: '.com.py',
  full: 'educacion.com.py',
} as const;
