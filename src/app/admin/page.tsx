import { PagePlaceholder } from '@/components/layout/PagePlaceholder';

/** Placeholder only — auth and the real admin CRUD ship in PR-18/PR-19. */
export default function AdminPage() {
  return <PagePlaceholder title="Admin" />;
}
