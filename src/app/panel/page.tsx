import { PagePlaceholder } from '@/components/layout/PagePlaceholder';

/** Placeholder only — auth and the real institution portal ship in PR-18/PR-21. */
export default function PanelPage() {
  return <PagePlaceholder title="Panel de institución" />;
}
